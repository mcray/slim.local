<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-controls="navbar">
                <span class="sr-only">Адаптация меню</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path_for('home') }}">Мой сайт на Slim 3</a>
        </div>

        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path_for('home') }}">Главная</a></li>
            </ul>
            <ul class="nav navbar-nav navbar-right">
                 {% if auth.check %}
                     <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ auth.user.name }}<span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path_for('auth.password.change') }}">Изменить пароль</a></li>
                            <li><a href="{{ path_for('auth.signout') }}">Выход</a></li>
                        </ul>
                    </li>
                {% else %}
                    <li><a href="{{ path_for('auth.signup') }}">Регистрация</a></li>
                    <li><a href="{{ path_for('auth.signin') }}">Вход</a></li>
                {% endif %}
            </ul>
        </div>

    </div>
</nav>
